{
  "name": "y",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "hai-server --watch database.json --port 3000",
    "start_with_auth": "hai-server --watch database.json --auth auth_db.json --port 3000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "hai-server": "^0.0.4"
  }
}
